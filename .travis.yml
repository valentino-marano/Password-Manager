language: java
jdk:
  - oraclejdk8
services:
  - docker
install:
  - docker pull mongo
sudo: true
cache:
  directories:
  - $HOME/.m2
script: 
  - mvn -f com.valentino.tap.password_manager/pom.xml clean verify -Pjacoco coveralls:report
